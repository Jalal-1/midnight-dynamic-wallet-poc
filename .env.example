# Public browser configuration. Copy this file to .env.local and replace the
# placeholder with the ID for your own Dynamic environment. These values are
# browser-visible; never put API tokens, OAuth secrets, or wallet keys here.
NEXT_PUBLIC_DYNAMIC_ENVIRONMENT_ID=replace-with-your-dynamic-environment-id
NEXT_PUBLIC_APP_URL=http://localhost:3000
